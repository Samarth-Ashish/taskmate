import 'package:flutter/material.dart';

class SleepStatsPage extends StatelessWidget {
  const SleepStatsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Sleep Stats')));
  }
}
